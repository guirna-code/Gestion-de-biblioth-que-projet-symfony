{% extends 'base.html.twig' %}

{% block title %}Bibliothèque{% endblock %}

{% block body %}
    <div class="container">
        <div class="page-header">
            <div>
                <h1 class="page-title">📚 Bibliothèque</h1>
                <p class="page-subtitle">Gestion complète de votre bibliothèque numérique</p>
            </div>
            <div class="flex items-center gap-md">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <span class="badge badge-success">Connecté : {{ app.user.email }}</span>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline btn-sm">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-primary btn-sm">Se connecter</a>
                {% endif %}
            </div>
        </div>

        <div class="grid grid-3 gap-lg">
            <div class="card card-icon">
                <div class="icon">📘</div>
                <div>
                    <h3 class="card-title">Livres</h3>
                    <div class="flex items-center justify-between mt-md">
                        <div>
                            <h2 id="count-books" class="mb-0">—</h2>
                            <small class="text-muted">Total livres</small>
                        </div>
                        <a href="{{ path('app_book_index') }}" class="btn btn-primary btn-sm">Gérer</a>
                    </div>
                </div>
            </div>
            {# ... (autres cartes stats) #}
        </div>

        <div class="mt-xl">
            <div class="flex items-center justify-between mb-md">
                <h2 class="h4 mb-0">📖 Catalogue des livres</h2>
            </div>

            <div class="grid grid-3 gap-md">
                {% for livre in livres %}
                    <div class="card h-full flex flex-col justify-between">
                        <div>
                            {# CORRECTION ICI : Utilisation de .titre au lieu de .title #}
                            <h3 class="h5 mb-sm">{{ livre.titre|default('Titre inconnu') }}</h3>
                            <p class="text-muted small">Auteur : {{ livre.auteur|default('Auteur inconnu') }}</p>
                        </div>
                        <div class="mt-md">
                            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                <a href="{{ path('app_achat_confirmation', {'id': livre.id}) }}" 
                                  class="btn btn-primary btn-block">
                                     Commander maintenant
                                 </a>
                            {% else %}
                                <a href="{{ path('app_login') }}" class="btn btn-outline btn-block btn-sm">Connectez-vous pour commander</a>
                            {% endif %}
                        </div>
                    </div>
                {% else %}
                    <div class="card col-span-3 center p-lg">
                        <p class="muted">Aucun livre n'est disponible pour le moment.</p>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>

    {# ... (bloc javascripts identique) #}
{% endblock %}